# 船舶码头船员休息室配套电器房间占用关联登记系统

## 项目简介

本系统是一个港口后勤管理系统，主要管理休息室的冰箱、热水器等电器设备。核心功能为**房间-船舶双维度关联绑定**，实现电器档案同时绑定休息室编号与对应停靠船舶；支持船舶换班、休息室重新分配时同步修改两层关联关系；支持按船舶编号汇总该船配套全部休息室电器；支持按休息室查看当前绑定船舶信息。

## 技术栈

- **前端**: Vue3 + Vite + TypeScript + Element Plus + Vue Router + Axios
- **后端**: Spring Boot 3.3 + JDK 17 + Maven + JPA + Spring Data Redis
- **数据库**: MySQL 8.0
- **缓存**: Redis 7.2 (SortedSet缓存电器功率参数)
- **容器化**: Docker + Docker Compose

## 端口配置

| 服务 | 端口 |
|------|------|
| 前端 | 8122 |
| 后端 | 8132 |
| MySQL | 3348 |
| Redis | 6420 |

## 访问地址

- **前端**: http://localhost:8122
- **后端API**: http://localhost:8132/api

## 核心功能模块

1. **休息室电器基础建档**：设备编号、功率、电器类型管理
2. **船舶+休息室双重绑定**：同步登记设备所属船舶与休息室
3. **双维度关联关系同步调整**：船舶换班、休息室重新分配时同步修改两层关联关系，留存变更记录
4. **双向反向汇总**：按船舶/休息室查询配套电器清单

## 启动方式

### 开发环境

```bash
docker compose -f docker-compose.dev.yml up -d --build
```

### 生产环境

```bash
docker compose up -d --build
```

### 一键启动脚本

```bash
./start.sh
```

## 验证结果

### 后端单独编译
- 状态：系统无Java环境，跳过（Docker构建时已验证）
- Docker构建：✅ 通过

### 前端单独构建
- 状态：✅ 通过
- 命令：`npm ci && npm run build`

### Docker整体构建启动
- 状态：✅ 通过
- 命令：`docker compose up -d --build`

### localhost与127.0.0.1验证
- localhost:8122：✅ 返回前端页面
- 127.0.0.1:8122：✅ 返回前端页面

### 浏览器页面和中文显示验证
- 页面加载：✅ 正常
- 中文显示：✅ 正常（菜单显示：电器管理、休息室管理、船舶管理、关联绑定、统计查询、变更记录）

## 项目路径

```
/Users/Admin/Desktop/solo-0601/qyx-0701/qyx-组1/qyx-202/
├── backend/                    # Spring Boot后端
├── frontend/                   # Vue3前端
├── docker-compose.yml          # 生产环境Docker Compose
├── docker-compose.dev.yml      # 开发环境Docker Compose
├── .env                        # 环境变量配置
├── .gitignore                  # Git忽略配置
├── .dockerignore               # Docker忽略配置
├── start.sh                    # 启动脚本
├── README.md                   # 项目说明
└── qyx-202.md                  # 项目交付文档
```

## 镜像配置

- **前端**: 使用淘宝npm镜像 (`https://registry.npmmirror.com/`)
- **后端**: 使用阿里云Maven镜像 (`https://maven.aliyun.com/repository/public/`)

## 数据库表结构

- `electric_appliance` - 电器表
- `lounge_room` - 休息室表
- `ship` - 船舶表
- `room_ship_relation` - 房间船舶关联表
- `relation_change_log` - 关联变更记录表（独立存储变更流水）
- `lounge_key` - 休息室钥匙档案表（钥匙 → 所属休息室）
- `key_occupancy` - 钥匙占用表（key_id 唯一约束；一次领取同时落下钥匙跟哪间房、房间今晚停靠哪条船）
- `key_checkout_record` - 钥匙领还流水表（与占用同事务、同批次写入）
- `linen_recovery` - 布草回收单表（一房一次换船周期一份；active_unique_key = roomId#changeBatch 唯一约束兜底一房一周期只挂一份未作废单）

## 钥匙领还（本次新增）

### 一次领取 = 一次事务

`POST /api/key/checkout` 在单个事务内依次：取换班全局行锁（与换班操作互斥）→ 悲观锁钥匙行 → 校验钥匙未被领走 → 悲观锁房间及其停靠关联 → 以**锁定后的当前 ACTIVE 关联**校验「领取按的船舶 = 房间此刻停靠船舶」→ 同一批次号同时写入 `key_occupancy`（占用）与 `key_checkout_record`（OUT 流水）。任一环节失败整个事务回滚，占用与快照都不留半写。

- 房间已换船仍按旧船领取 → 409 整单退回，响应携带卡住的钥匙、房间、按的旧船、当前停靠船
- 两人同时领同一把钥匙 → 钥匙行悲观锁串行化 + `key_occupancy.key_id` 唯一约束兜底，后到者 409 并看到当前持匙人
- 归还 `POST /api/key/return` 同事务删除占用并把同批次流水置为 RETURNED

### 三处对账（同一次领取）

- 按船看未还钥匙：`GET /api/key/occupancy/ship/{shipId}`
- 按房看持匙人：`GET /api/key/occupancy/room/{roomId}`
- 领还流水：`GET /api/key/records?keyId=&roomId=&shipId=`

三处均读取同一事务写入的占用/流水（同一 `checkout_batch` 对账），关掉页面重开仍从 MySQL 对成同一次领取。

### 前端

- 菜单新增「钥匙领还」(`/key`)：钥匙档案、领取（选钥匙自动带出所属房间与其当前停靠船舶）、归还
- 对账三页签：按船看未还钥匙 / 按房看持匙人 / 领还流水（可按钥匙、房间、船舶过滤）
- 领取被退回时弹窗明示卡住的钥匙、房间、按的旧船与当前停靠船

## 夜班巡检交班（本次新增）

### 一次交班 = 一次事务，且只有值班长能交

`POST /api/patrol/handover` 在单个事务内：取换班全局行锁 → 锁接班窗口行、船舶行 → 校验提交人是本班在岗值班长（非值班长 403）→ 以锁定后的当前 ACTIVE 靠泊关系得到该船当时挂靠的全部房间 → 校验走房条目。必须**按楼层从低到高、一间不漏**：漏房 / 跳层 / 乱序重复 / 含已改挂别船的房间，一律 409 整单退回。

- 校验通过：同事务写交班流水 `patrol_handover`（值班长、窗口、走房顺序、走房数）和每间房走房勾 `patrol_check`（同批次、顺序序号、楼层快照）。
- 校验不通过：只写一条 `BLOCKED` 退回说明（卡住房间、楼层、该房当前停靠船、窗口），**不写任何走房勾**，不留半截。
- 接班窗口 `patrol_window` 起止时间建立后不可改；窗口未开始 / 已截止 / 已关闭提交返回 423，不能再补勾或改交班。
- 两人同时给同船同窗口交班：全局行锁串行 + `handed_unique_key = shipId#windowId` 唯一约束兜底，最多一份 `HANDED`。

### 状态对账（关掉页面重开仍一致）

`GET /api/patrol/state?windowId=&shipId=` 返回未交 `NOT_HANDED` / 卡在漏房跳层脱挂 `BLOCKED`（带卡住房间与窗口）/ 窗口外已锁 `LOCKED` / 已交 `HANDED`；`GET /api/patrol/records` 查交班流水。

### 前端

- 菜单新增「夜班巡检交班」(`/patrol`)：选船与接班窗口，展示该船当时挂靠房间的应巡顺序（楼层低→高）与当前状态。
- 交班弹窗按楼层列出应巡房间供值班长勾选，序号按楼层顺序生成；退回时弹窗明示卡住窗口、卡住房间、跳层来源 / 当前停靠船与原因。
- 窗口维护页签可建立窗口（起止时间不可改）、提前关闭；窗口外按钮禁用并提示已锁。

## 布草回收（本次新增）

解决换船后保洁把脏床单扔门口就下班、值班本只写「已打扫」、套数/封袋重量/见证人对不上的问题。

### 一房一周期一页回收单

每间房**每换过一次船**开一页回收单，单子锁定在本次换班批次（`change_batch`）上，三栏：收走的脏床品**套数**、秤上的**封袋公斤数**、**见证人姓名**。

- `POST /api/linen/records`：三栏允许先空（`DRAFT` 草稿，离泊以后补登入口照常可用）；三栏齐时当场按**每套约定公斤区间 1.50~2.50kg/套**（随单留底）对账。
  - 三栏没齐：只落草稿，**可住灯不亮**，房间亮「回收未齐」。
  - 三栏齐且 `套数×每套区间` 包住本次秤重：保存即 `CONFIRMED`，**可住灯亮**。
  - 公斤折套数对不上：整单 409 退回，失败说明带「每套约定区间、这次秤重、按公斤折出来的套数（ceil(重量/每套上界) ~ floor(重量/每套下界)）」。
- 可住灯已亮（`CONFIRMED`）后，**套数、公斤数锁死**，谁再改歪保存 409 失败（`TAMPER_AFTER_CONFIRMED`），同样带区间、秤重、折套数；要纠正只能先 `PUT /api/linen/records/{id}/void` 作废再重开（作废后可住灯随之熄灭）。
- 同一间房同一换班周期最多一份未作废回收单：取换班全局行锁 + 房间行锁串行，`active_unique_key = roomId#changeBatch` 唯一约束兜底，两人同时保存后到者 409，并带**已经在库的回收单号**。

### 占用闸门

`POST /api/key/checkout` 在船舶校验通过后追加布草闸门 `assertAvailableForOccupancy`：

- 房间没换过船（首班）放行；
- 换过船但回收单缺着（没单 / 草稿三栏未齐 / 未确认）→ 409 整单退回，别的船的人不能领钥匙占用；
- 已确认（三栏齐、对得上）才放行。

### 灯与卡片（二者互斥，验收看三栏）

`GET /api/linen/rooms`（单间 `GET /api/linen/rooms/{roomId}`）返回每间房 `NOT_NEEDED / PENDING / DRAFT / RECOVERED` 与 `availableLight`、`recoveryPendingLight`——可住灯与「回收未齐」由后端保证**绝不并排亮**。房间卡片只有 `RECOVERED` 才画「已换洗」对勾，且对勾旁必须摆出套数、封袋公斤、见证人；只画对勾、没有套数公斤和见证人的状态（草稿/缺单）不显示对勾，过不了验收。`GET /api/linen/records?roomId=&shipId=` 查回收单流水。

### 前端

- 菜单新增「布草回收」(`/linen`)：房间卡片一张房一卡，顶部一枚互斥灯（🟢可住 / 🔴回收未齐 / ⚪无需回收），卡片摆当前停靠船、上一班（离泊）船、布草状态与三栏明细。
- 「新开回收单 / 补登」弹窗填套数、封袋公斤、见证人；退回弹窗明示约定区间、本次秤重、折出来的套数；已确认卡片只给锁定提示与「作废」。
- 钥匙领取被回收闸门挡回时，弹窗明示卡住房间、上一班船、未齐回收单号与每套约定，引导先到「布草回收」补齐。

## 发电机加油（本次新增）

解决码头楼顶应急发电机加完油本子上只写「加过了」，对不上加了多少升、哪一罐来的，第二天盘库存对不齐的问题。

### 登记：机、罐、升数、经办，一样不能少

`POST /api/fuel/records`：选中哪一台机，写下**本罐编号**、**实加升数**、**经办值班**（姓名+班次：早班/中班/夜班）。缺罐号、缺升数、缺经办一律 400 退回——光写「加过了」入不了库。登记后状态 `PENDING_REVIEW`（待复核），**库存升数暂不变化**。

- 同一台机在同一自然日只允许挂着**一条**未复核的加油：换班全局行锁 + 发电机行锁串行，`active_unique_key = generatorId#refillDate` 唯一约束兜底，两人同时登记后到者 409，并带**已在库那条的单号、罐号、升数、经办**。
- 复核通过之后，这台机当天才能再开下一条（`active_unique_key` 随复核通过清空）。

### 复核：另一个班的人核，与库存升数同事务

`POST /api/fuel/records/{id}/review`：

- 复核人必须是**另一个班**的人，**自己加的不能自己核**（同人不同班也不行）——违反一律 403。
- 复核通过与库存升数**同一事务**：先把实加升数加进发电机库存，再落复核结论（复核人、班次、时间、意见、库存前→后快照随单留底）。复核写到一半失败整体回滚——升数和复核结论不留半截，库存升数仍是复核前的数。
- 两人抢着复核同一条：全局行锁 + 记录行悲观锁串行，**只留先写完的那次结论**；后到的人 409，并看得见这条已经核过（复核人、班次、时间）。

### 列表与流水（「已加油」列背后必须带得出升数和复核人）

`GET /api/fuel/generators/state`：每台机的库存升数、今日状态（`NONE` 未加油 / `PENDING_REVIEW` 待复核 / `REVIEWED` 已加油）、今日已核升数、复核人（班次）、待复核单明细。`GET /api/fuel/records?generatorId=&date=` 查加油流水：单号、机、日期、罐号、实加升数、经办（班）、复核人（班）、复核时间、库存前→后。只在列表加一列「已加油」而没有可查的升数和复核人，不算做完。

### 前端

- 菜单新增「发电机加油」(`/fuel`)：发电机列表带「已加油」列（已加油/待复核/未加油）、库存升数、今日已核升数、复核人、待复核单明细；「发电机建档」维护机具档案与期初库存。
- 「登记加油」弹窗填本罐编号、实加升数、经办值班与班次；「复核」弹窗摆出待复核单明细，填复核人与班次；登记撞单 / 复核撞车 / 自加自核被退回时，弹窗明示已在库单号或先写完的那次复核结论。
